# Agent notes

## Git attribution

Commits and PRs must be attributed only to **Ali Hamza** / the repo owner.

- Never add `Co-authored-by: Cursor` or `Made-with: Cursor`
- See `.cursor/rules/git-attribution.mdc`
- Local hook: `git config core.hooksPath .githooks` (strips Cursor trailers on commit)

## Writing

- No em dashes (`—`) in copy, docs, UI, or chat. Use `-`, `:`, or a new sentence.
- See `.cursor/rules/no-em-dashes.mdc`
